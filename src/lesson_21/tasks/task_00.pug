extends ../../layouts/_task-layout.pug

block frontmatter
  include ../_collection
  -
    const currentTask = taskGroups
      .flatMap((group) => group.tasks)
      .find((task) => task.id === 0)

append scripts
  script(defer src="./scripts/task_00.js")

block content
  p= currentTask.description
  hr
  h2 Рішення:
  .js-app
  include:code(lang="javascript") ./scripts/task_00.js
