extends ../../layouts/_task-layout.pug

block frontmatter
  include ../_collection
  -
    const currentGroup = taskGroups.find((group) =>
      group.tasks.find((task) => task.id === 47),
    )
    const currentTask = currentGroup.tasks.find((task) => task.id === 47)

block content
  p!= currentTask.description
  hr
  h2 Рішення:
  script(src="./scripts/task_47.js")
