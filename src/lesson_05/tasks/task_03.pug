extends ../../layouts/_task-layout.pug

block frontmatter
  include ../_collection
  -
    const currentTask = tasks.find(task=>task.number === 03)

block content
  p Вивести таблицю з одним рядком і 7 стовпцями.
  hr
  h2 Рішення:
  script(src="./scripts/task_03.js")
