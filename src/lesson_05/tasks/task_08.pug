extends ../../layouts/_task-layout.pug

block frontmatter
  include ../_collection
  -
    const currentGroup = taskGroups.find((group) =>
      group.tasks.find((task) => task.id === 08),
    )
    const currentTask = currentGroup.tasks.find((task) => task.id === 08)


block content
  p Користувач загадує число. За 3 спроби комп'ютер намагається вгадати число користувача (використати confirm).
  hr
  h2 Рішення:
  script(src="./scripts/task_08.js")
