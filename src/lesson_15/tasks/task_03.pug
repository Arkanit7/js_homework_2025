extends ../../layouts/_task-layout.pug

block frontmatter
  include ../_collection
  -
    const currentTask = taskGroups
      .flatMap((group) => group.tasks)
      .find((task) => task.id === 3)


block content
  p= currentTask.description
  table
    tbody
      tr
        td(rowspan="3") Поля
        td Число, яке перевіряємо (наприклад, перевірка частини таблиці множення на 7)
      tr
        td Кількість правильних відповідей
      tr
        td Кількість неправильних відповідей

      tr
        td(rowspan="3") Методи
        td Генерування прикладу (метод випадковим чином визначає друге число, перше число фіксоване)
      tr
        td Перевірка правильності вказаної відповіді
      tr
        td render - виведення інформації про тестування на екран

  hr
  h2 Рішення:

  script(src="./scripts/task_03.js")

  include:code(lang="javascript") ./scripts/task_03.js
